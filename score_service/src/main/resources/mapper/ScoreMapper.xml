<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cwh.score.mapper.ScoreMapper">

    <insert id="saveUserScore" parameterType="java.util.Map">
    	insert into t_score values(#{id},#{userId},#{score})
    </insert>
</mapper>